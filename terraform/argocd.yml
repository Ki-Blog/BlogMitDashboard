apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aiq
spec:
  source:
    repoURL: "https://github.com/kpblmMik/AI-Quantum-Fork.git"
    path: "./deployment"
    targetRevision: "CI"
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  project: default
  syncPolicy:
    automated:
      prune: true
