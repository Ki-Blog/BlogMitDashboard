name: Cypress Tests

on:
  push:
    branches:
      - feature/cypress

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
     
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Change directory to e2e-testing
        run: cd e2e-testing

      - name: Install npm dependencies
        run: npm install

      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          project: ./  # Assuming Cypress is configured within e2e-testing directory
          spec: cypress/e2e/aiq-blog.cy.js
