apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "BlogDashborad-chart.fullname" . }}-test-connection"
  labels:
    {{- include "BlogDashborad-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "BlogDashborad-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
