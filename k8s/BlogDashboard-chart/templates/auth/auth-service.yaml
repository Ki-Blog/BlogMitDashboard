# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: aiqauth
  namespace: default
spec:
  selector:
    app: aiqauth
  type: ClusterIP
  ports:
    - name: aiqauth
      protocol: TCP
      port: {{ .Values.aiqauth.port}}
      targetPort: {{ .Values.aiqauth.targetPort }}

