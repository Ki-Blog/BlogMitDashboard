apiVersion: v1
kind: Service
metadata:
  name: aiq-user
  namespace: default
spec:
  selector:
    app: aiq-user
  type: {{ .Values.aiq-user.service.type }}
  ports:
    - name: aiq-user
      protocol: TCP
      port: {{ .Values.aiq-user.service.port }}
      targetPort: {{ .Values.aiq-user.service.targetPort }}
