apiVersion: v1
kind: Secret
metadata:
  name: shared-secrets
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  MONGO: {{ .Values.sharedSecrets.MONGO | b64enc }}
  JWT_SECRET: {{ .Values.sharedSecrets.JWT_SECRET | b64enc }}
